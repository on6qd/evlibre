package com.evlibre.server.core.domain.v201.model;

/**
 * OCPP 2.0.1 {@code MessageFormatEnumType} — the encoding of a piece of
 * driver-facing display content (used by {@link MessageContent}).
 */
public enum MessageFormat {
    ASCII,
    HTML,
    URI,
    UTF8
}
